{
  "name": "ceac-beautiful-checker-backend",
  "version": "1.0.0",
  "description": "Python backend for the CEAC Visa Status Checker",
  "scripts": {
    "install-deps": "pip3 install playwright python-dotenv google-generativeai onnxruntime numpy pillow && python3 -m playwright install chromium",
    "start": "python3 ceac_checker.py"
  },
  "dependencies": {
    "playwright": "*",
    "python-dotenv": "*",
    "google-generativeai": "*",
    "onnxruntime": "*",
    "numpy": "*",
    "pillow": "*"
  }
}
